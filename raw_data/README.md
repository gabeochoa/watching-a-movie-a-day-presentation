# raw_data/

Put **only the original inputs** here (no edits).

- Letterboxd export zip: `raw_data/letterboxd-export.zip`
- Optional: any manual notes you want to reference while writing the deck

This folder is intended to be **local/private** (see `.gitignore`).

